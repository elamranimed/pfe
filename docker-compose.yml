services:
  db:
    image: postgres:15
    container_name: syndic_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Hello2U!qc
      POSTGRES_DB: test_db
    ports:
      - '5432:5432'    
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: